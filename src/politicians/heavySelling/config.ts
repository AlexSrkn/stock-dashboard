/** Default calendar window for multi-politician selling detection. */
export const DEFAULT_MULTIPLE_SELLERS_WINDOW_DAYS = 30;

/** Minimum unique politicians selling the same stock in the window. */
export const DEFAULT_MULTIPLE_SELLERS_MIN = 3;
