/** Presentational helpers for Tools → Find Similar Stocks. */

export function similarStocksPageTitle(): string {
  return "Find Similar Stocks";
}

export function similarStocksMethodology(): string {
  return "Similarity ranks stocks by filing-based institutional profile, holder overlap, activity patterns, insider and politician behavior, and signal presence — without using price history, fundamentals multiples, or analyst ratings.";
}
